# ۴. سه لایه: الگوریتم، شبیه‌سازی، ابزار

## جایگاه در نقشه

این فصل ستون مفهومی کل سند است. درهم‌ریختگی پژوهشی معمولاً از قاطی کردن این سه لایه می‌آید. جدول عملی «اگر X می‌خواهید…» در [00-map.md](00-map.md) است.

## ایدهٔ شهودی

سه مفهوم نزدیک‌اند ولی یکی نیستند.

### لایهٔ ۱ — الگوریتم کوانتومی

دستورالعمل محاسباتی برای حل یک مسئله. مثال: «با این مدار، انرژی پایه را تا خطای \(\varepsilon\) برآورد کن.» الگوریتم می‌گوید *چه کارهایی به چه ترتیبی* انجام شود.

### لایهٔ ۲ — شبیه‌سازی کوانتومی

استفاده از یک سامانهٔ کوانتومی برای بازنمایی و مطالعهٔ یک سامانه یا مدل کوانتومی دیگر. اینجا هدف پژوهشی است: مدل هابارد، یک مولکول، یک نظریهٔ پیمانه‌ای شبکه‌ای (lattice gauge theory). شبیه‌سازی می‌تواند دیجیتال، آنالوگ، یا ترکیبی باشد. شبیه‌سازی یک گیت خاص نیست.

### لایهٔ ۳ — روش‌های پیاده‌سازی

ابزارهایی برای تحقق یک هدف محاسباتی مشخص. Trotter، ترکیب خطی یکانی‌ها (LCU)، پردازش سیگنال کوانتومی (QSP)، برآورد فاز (QPE)، VQE، و تحول آدیاباتیک اینجا نشسته‌اند.

جملهٔ جهت‌یاب: *شبیه‌سازی می‌گوید چه مدلی را مطالعه می‌کنیم؛ الگوریتم می‌گوید با چه روالی؛ ابزار می‌گوید آن روال را چطور روی دستگاه می‌سازیم.*

## حداقل فرمالیسم

| می‌خواهید… | لایه | نمونه |
|---|---|---|
| مسئله را نام ببرید | ۲ | دینامیک هابارد پس از تغییر ناگهانی هامیلتونی (quench) |
| روال حل را بنویسید | ۱ | آماده‌سازی \(\lvert\psi_0\rangle\)، اعمال \(U(t)\)، اندازه‌گیری \(O\) |
| \(U(t)\) را بسازید | ۳ | فرمول حاصل‌ضربی (product formula) یا QSP |
| ویژهٔمقدار را بخوانید | ۱+۳ | الگوریتم QPE که زیرروالش شبیه‌سازی کنترل‌شده است |
| حالت پایه روی NISQ | ۱+۳ | VQE: آنزات وردشی + بهینه‌ساز کلاسیک |

یک ابزار می‌تواند در چند الگوریتم ظاهر شود. Trotter هم دینامیک می‌سازد، هم مسیر آدیاباتیک دیجیتال، هم ورودی QPE.

## ارتباط

- مدل اجرا: [03-quantum-model.md](03-quantum-model.md)
- مدل‌های فیزیکی لایهٔ ۲: [05-physical-models.md](05-physical-models.md)
- رهیافت‌ها: [08-evolution-vs-adiabatic.md](08-evolution-vs-adiabatic.md)
- ابزار دیجیتال: [09-digital-primitives.md](09-digital-primitives.md)
- ابزار طیفی: [10-spectral-tools.md](10-spectral-tools.md)

## چه وقت استفاده می‌شود

قبل از خواندن هر مقاله بپرسید: این کار مسئله است، الگوریتم است، یا ابزار؟ اگر هر سه را در یک جمله قاطی کردید، به این فصل برگردید.

## جای توسعه

- تمایز شبیه‌سازی آنالوگ Feynman در برابر شبیه‌سازی دیجیتال Lloyd.
- جایگاه نمونه‌برداری بوزونی (boson sampling) به‌عنوان مسئله‌ای مجزا از شبیه‌سازی هامیلتونی.
- جدول مقالات شاخص هر لایه.
