# ۵. از مسئلهٔ فیزیکی تا هامیلتونی

## جایگاه در نقشه

بالای نمودار: Physical problem → Mathematical model → Hamiltonian \(H\). قبل از انتخاب Trotter یا VQE، باید معلوم شود \(H\) چیست و جمله‌هایش چه ساختاری دارند.

## ایدهٔ شهودی

فیزیک خام (یک ماده، یک مولکول، یک میدان) مستقیماً الگوریتم نمی‌شود. نخست مدلی می‌سازیم که درجات آزادی را نگه می‌دارد و بقیه را دور می‌ریزد. خروجی این مرحله تقریباً همیشه یک هامیلتونی است:

\[
H = \sum_j h_j
\]

که هر \(h_j\) موضعی یا کم‌وزن است. از اینجا به بعد، مسیر الگوریتمی برای مدل‌های خیلی متفاوت شبیه هم می‌شود. آنچه عوض می‌شود:

- **کدگذاری** درجات آزادی روی کیوبیت (یا مد بوزونی)
- **تعداد و نوع جمله‌ها** (پائولی، فرمیونی، پیمانه‌ای)

چهار خانواده برای جهت‌یابی کافی‌اند. هیچ‌کدام را اینجا حل نمی‌کنیم.

### اسپین: Ising و Heisenberg

ساده‌ترین ورود. درجات آزادی اسپین روی شبکه؛ \(H\) جمع جمله‌های دو جسمی مثل \(Z_i Z_j\) یا \(\vec{S}_i\cdot\vec{S}_j\). شهود: مغناطیس، شیشهٔ اسپینی، و بسیاری مسائل بهینه‌سازی هم به Ising نگاشته می‌شوند.

### هابارد (فرمی و بوز)

مدل استاندارد همبستگی قوی: ذرات روی شبکه می‌پرند (\(t\)) و اگر روی یک سایت باشند انرژی \(U\) می‌پردازند. فرمی-هابارد الکترون‌های همبسته است؛ بوز-هابارد اتم‌های سرد در شبکهٔ نوری. پرسش‌های نوعی: عایق مات، ابرشاره، ترابرد (transport)، دینامیک پس از تغییر ناگهانی هامیلتونی.

### ساختار الکترونی مولکولی

الکترون‌ها در میدان هسته‌ها. پس از انتخاب پایه و اغلب یک فضای فعال (active space)، \(H\) به شکل فرمیونی یا پائولی درمی‌آید. شیمی کوانتومی از همین‌جا به VQE و به شبیه‌سازی تحمل‌پذیر در برابر خطا وصل می‌شود.

### میدان روی شبکه و نظریهٔ پیمانه‌ای شبکه‌ای

میدان کوانتومی پیوسته را روی شبکه می‌نشانند تا درجات آزادی متناهی شود. نظریه‌های پیمانه‌ای شبکه‌ای (lattice gauge theory) قیدهای موضعی (قانون گاوس) دارند؛ همین قیدها کدگذاری را سخت‌تر از هابارد می‌کنند. پرسش‌ها: طیف هادرونی، دینامیک خارج از تعادل، گذار فاز.

## حداقل فرمالیسم

شکل نوعی:

- اسپین: \(H = \sum_{\langle ij\rangle} J_{ij}\, \sigma_i\cdot\sigma_j + \sum_i h_i\sigma_i^z\)
- هابارد: \(H = -t\sum_{\langle ij\rangle\sigma}(c_{i\sigma}^\dagger c_{j\sigma}+\mathrm{h.c.}) + U\sum_i n_{i\uparrow}n_{i\downarrow}\)
- مولکول: \(H = \sum_{pq} h_{pq} a_p^\dagger a_q + \tfrac12\sum_{pqrs} h_{pqrs} a_p^\dagger a_q^\dagger a_r a_s\)

نکتهٔ مشترک: پس از \(H\)، سؤال پژوهشی به فصل [۷](07-dynamics-vs-spectrum.md) می‌رود نه به «نوع ماده».

## ارتباط

- سه لایه: [04-three-layers.md](04-three-layers.md)
- جریان کاری با مثال هابارد: [06-pipeline.md](06-pipeline.md)
- رقبا کلاسیک: [02-classical-algorithms.md](02-classical-algorithms.md)
- کدگذاری و سخت‌افزار: [11-analog-hybrid.md](11-analog-hybrid.md)

## چه وقت استفاده می‌شود

وقتی مقاله یا ایدهٔ تازه‌ای دیدید، اول بپرسید مدل کدام است و \(H\) چند جمله و از چه نوعی دارد. قبل از انتخاب الگوریتم.

## جای توسعه

- نگاشت‌های فرمیون به کیوبیت (Jordan–Wigner، Bravyi–Kitaev).
- مدل‌های ارتعاشی--الکترونی (vibronic) و مد بوزونی.
- شکافت سینگلت و مسائل فوتوفیزیک به‌عنوان مثال کاربردی.
- جزئیات قید پیمانه‌ای و حذف درجهٔ آزادی اضافی.
