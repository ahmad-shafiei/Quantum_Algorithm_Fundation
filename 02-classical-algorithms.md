# ۲. خانواده‌های کلاسیکِ مرتبط

## جایگاه در نقشه

هنوز در لایهٔ کلاسیک هستیم. هدف، کاتالوگ الگوریتم‌های عمومی نیست. فقط خانواده‌هایی که بعداً با شبیه‌سازی کوانتومی یا الگوریتم کوانتومی **تلاقی مفهومی** دارند.

## ایدهٔ شهودی

هر بار که یک الگوریتم کوانتومی می‌بینید، معمولاً جانشین یا رقیب یک روال کلاسیک است. اگر آن رقیب را نشناسید، نمی‌فهمید کوانتوم دقیقاً چه چیزی را عوض کرده: مدل هزینه را، نمایش حالت را، یا خودِ سؤال را.

پنج خانواده برای این سند کافی‌اند.

### جستجو و بهینه‌سازی

جستجوی ناساخت‌یافته و بهینه‌سازی ترکیبیاتی. در کوانتوم: Grover، QAOA، و بازپخت کوانتومی. سؤال این خانواده «یافتن یک شیء یا یک کمینه» است، نه الزاماً شبیه‌سازی یک \(H\).

### تبدیل فوریه

از حوزهٔ زمان/مکان به بسامد. نسخهٔ کوانتومی آن QFT است و ستون برآورد فاز. اگر فوریه را فراموش کنید، QPE مثل یک جعبهٔ سیاه می‌ماند.

### دستگاه خطی و زیرفضای Krylov

حل \(Ax=b\) و استخراج اطلاعات طیفی با تکرارهای ماتریسی. در کوانتوم: HHL و به‌طور کلی‌تر توابعی از عملگر از راه block-encoding. شبیه‌سازی هامیلتونی هم از همین جنس است: اعمال تابعی از \(H\).

### نمونه‌برداری و مونت‌کارلو

برآورد یک مقدار با نمونه‌های تصادفی. نسخهٔ کوانتومی اغلب برآورد دامنه است. در فیزیک بس‌ذره‌ای (many-body)، مونت‌کارلو کوانتومی خودش یک رقیب کلاسیک جدی برای شبیه‌سازی است.

### شبیه‌سازی کلاسیک سامانه‌های کوانتومی

این مهم‌ترین رقیب مستقیم پژوهش شماست:

- **قطری‌سازی دقیق** و شبیه‌سازی بردار حالت (state vector simulation): دقیق، نمایی در تعداد ذرات.
- **مونت‌کارلو کوانتومی:** گاهی مقیاس بهتر، با محدودیت علامت.
- **شبکهٔ تانسوری** (tensor network) و حالت حاصل‌ضرب ماتریسی (matrix product state)، از جمله DMRG: قوی برای درهم‌تنیدگی محدود.

الگوریتم کوانتومی وقتی معنا دارد که یکی از این مسیرها از نظر مقیاس، دقت، یا نوع سؤال ناکافی شود.

## حداقل فرمالیسم

برای جهت‌یابی، یک جمله کافی است: هزینهٔ کلاسیک شبیه‌سازی یک سامانهٔ کوانتومی عمومی معمولاً با بُعد فضای هیلبرت — یعنی به‌طور نمایی با تعداد ذرات — رشد می‌کند. استثناها همان روش‌هایی‌اند که ساختار (محلی بودن، نبود علامت، درهم‌تنیدگی کم) را استثمار می‌کنند.

## ارتباط

- تعریف الگوریتم: [01-algorithm.md](01-algorithm.md)
- مدل کوانتومی به‌عنوان جایگزین همین خانواده‌ها: [03-quantum-model.md](03-quantum-model.md)
- مدل‌های فیزیکی که این رقبا رویشان کار می‌کنند: [05-physical-models.md](05-physical-models.md)
- راستی‌آزمایی با حد کلاسیک: [12-closing-the-loop.md](12-closing-the-loop.md)

## چه وقت استفاده می‌شود

قبل از انتخاب یک الگوریتم کوانتومی بپرسید: «همین سؤال را کلاسیک تا چه اندازه و با چه روشی می‌توانم جواب بدهم؟»

## جای توسعه

- جدول مقیاس‌بندی کیفی: ED، MPS، QMC، مدار کوانتومی.
- جایگاه یادگیری ماشین کلاسیک برای مشاهده‌پذیرها.
- قانون مقیاس‌بندی (scaling law) هزینه در هر خانواده.
