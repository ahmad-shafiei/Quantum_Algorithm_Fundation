# ۹. جعبه ابزار دیجیتال: Trotter، LCU، QSP

## جایگاه در نقشه

زیرِ رهیافت تحول زمانی / \(f(H)\). این‌ها الگوریتم کاملِ «حل هابارد» نیستند؛ زیرروال ساختن یک تابع از هامیلتونی‌اند. خروجی‌شان مدار است.

## ایدهٔ شهودی

روی رایانهٔ دیجیتال نمی‌توان \(e^{-iHt}\) را به‌صورت یک دکمه داشت. باید آن را از گیت‌های ساده ساخت. سه نسل شهودی:

1. **Trotter:** \(H\) را تکه‌تکه اعمال کن.
2. **LCU:** اگر \(H\) جمع چند یکانی است، با کنترل و پس‌گزینش آن جمع را بساز.
3. **QSP:** وقتی یک بلوکِ کدگذاری‌شده از \(H\) دارید، تقریباً هر چندجمله‌ای از آن را با یک دنبالهٔ دوران بسازید.

هر سه پیاده‌کنندهٔ \(f(H)\) هستند. تحول زمانی \(f(x)=e^{-itx}\) فقط یک انتخاب است؛ فیلتر طیفی، معکوس، و تابع گرین انتخاب‌های دیگرند.

### Trotter — فرمول حاصل‌ضربی

اگر \(H=\sum_k H_k\) و هر \(e^{-iH_k\Delta t}\) آسان باشد،

\[
e^{-iH t}\approx \Bigl(\prod_k e^{-iH_k t/r}\Bigr)^r
\]

شهود: در زمان کوتاه، ترتیب اعمال تکه‌ها کمتر مهم است. هرچه \(r\) بیشتر، خطای تروتر (Trotter error) کمتر و مدار عمیق‌تر. ساده، شفاف، و نقطهٔ شروع تقریباً هر شبیه‌سازی دیجیتال.

### LCU — ترکیب خطی یکانی‌ها

اگر عملگر هدف ترکیب خطی چند یکانی باشد، می‌توان با یک ثبات کمکی، یک تبدیل روی ضرایب، و یک عمل کنترل‌شده آن را *بلوک‌کدگذاری* کرد. با اندازه‌گیری ثبات کمکی (و گاهی تکرار) بلوک درست را برمی‌دارید.

شهود: درِ ورود به الگوریتم‌های «جدیدتر» شبیه‌سازی. خودِ LCU اغلب به‌تنهایی تحول زمانی بهینه نیست، ولی زبان مشترکی برای گفتن «\(H\) را داخل یک مدار یکانی بزرگ گذاشتم» می‌دهد.

### QSP — پردازش سیگنال کوانتومی

وقتی \(H\) (یا دقیق‌تر: یک بلوک مرتبط با آن) داخل یک یکانی بزرگ نشسته، می‌توان با دنباله‌ای از دوران‌های تنظیم‌شده تقریباً هر چندجمله‌ای فرد/زوج مناسب از آن بلوک را پیاده کرد. QSVT تعمیم همین ایده به تبدیل‌های تکین است.

شهود: یک چارچوب واحد برای \(e^{-iHt}\)، فیلتر دور از طیف ناخواسته، و \(H^{-1}\). هزینه غالباً بهتر از Trotter ساده مقیاس می‌شود، ولی پیش‌نیازش همان بلوک‌کدگذاری است.

کیوبیتیزاسیون (qubitization) همسایهٔ طبیعی QSP است: یک پیاده‌سازی ساخت‌یافته از بلوک‌کدگذاری که پیمایش طیفی تمیزی به QSP می‌دهد. جزئیاتش جای توسعه است، نه فصل جدا در نسخهٔ اول.

## حداقل فرمالیسم

| ابزار | ورودی شهودی | خروجی | هزینهٔ شهودی | نقطهٔ ضعف |
|---|---|---|---|---|
| فرمول حاصل‌ضربی | تجزیهٔ \(H=\sum H_k\) | تقریب \(U(t)\) | رشد با \(t\) و تعداد جمله‌ها؛ توان خطا بسته به مرتبه | خطای تروتر؛ عمق زیاد در \(t\) بزرگ |
| LCU | \(H=\sum \alpha_i U_i\) | بلوک‌کدگذاری | وابسته به \(\sum\lvert\alpha_i\rvert\) و دقت | کیوبیت کمکی؛ پس‌گزینش |
| QSP / QSVT | بلوک‌کدگذاری \(H\) | چندجمله‌ای \(f(H)\) | نوعاً خطی در درجهٔ \(f\) | طراحی فازها؛ پیش‌نیاز بلوک |

جایگاه در پرسش‌ها: هر سه نخست برای دینامیک و \(f(H)\)اند. QPE از خروجی‌شان به‌عنوان \(U\) کنترل‌شده استفاده می‌کند.

## ارتباط

- رهیافت تحول: [08-evolution-vs-adiabatic.md](08-evolution-vs-adiabatic.md)
- مصرف‌کنندهٔ طیفی: [10-spectral-tools.md](10-spectral-tools.md)
- لایهٔ ابزار: [04-three-layers.md](04-three-layers.md)
- برآورد منابع: [12-closing-the-loop.md](12-closing-the-loop.md)

## چه وقت استفاده می‌شود

- شروع و شهود، یا \(H\) خیلی محلی: Trotter
- نیاز به زبان بلوک‌کدگذاری و الگوریتم‌های مدرن: LCU
- هدف یک \(f(H)\) عمومی با مقیاس بهتر در رژیم تحمل‌پذیر در برابر خطا: QSP/QSVT

## جای توسعه

- مرتبه‌های تروتر و کران خطای دقیق.
- randomized compiling / qDRIFT.
- مدار کیوبیتیزاسیون و انتخاب چندجمله‌ای QSP.
- مقایسهٔ asymptotic با یک جدول کوچک.
